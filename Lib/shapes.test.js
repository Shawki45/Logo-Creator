//circle shape
describe('Circle', () => {
    test('renders correctly', () => {
        const shape = new Circle();
        var color =('')
        shape.setColor(color);
        expect(shape.render()).toEqaual()
    }
    )
}
)